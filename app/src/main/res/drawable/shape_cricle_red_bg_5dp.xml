<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="oval">
    <solid android:color="@color/red_icon" />
    <size
        android:width="7dp"
        android:height="7dp" />

    <stroke
        android:width="0.8dp"
        android:color="@color/white" />
    <!-- 节点6：stroke (描边)
        android:width 描边的宽度
        android:color 描边的颜色
        把描边弄成虚线的形式"- - -":
        android:dashWidth 表示'-'这样一个横线的宽度
        android:dashGap 表示之间隔开的距离
    -->
</shape>